@{
    ViewData["Title"] = "Acceso denegado";
    Layout = "_LayoutSinNavbar";
}

<div class="login-wrapper">
    <div class="login-card text-center">
        <i class="bi bi-shield-lock-fill" style="font-size: 2.5rem; color: var(--danger);"></i>
        <h2 class="mt-3">Acceso denegado</h2>
        <p class="text-muted">No tenés permisos para acceder a esta sección.</p>
        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Volver al inicio</a>
    </div>
</div>
